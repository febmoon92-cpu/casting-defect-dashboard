# =====================================================================
# Casting Defect Dashboard – Streamlit Cloud용 dependencies (CPU only)
# Streamlit Cloud는 CPU 인스턴스이므로 PyTorch CPU 휠을 사용합니다.
# 로컬 GPU 학습/평가용 패키지(kaggle, jupyter 등)는 requirements-dev.txt 참고.
# =====================================================================

--extra-index-url https://download.pytorch.org/whl/cpu

# Deep learning (CPU wheels)
torch==2.4.1
torchvision==0.19.1

# Core scientific stack
numpy>=1.26,<2.0
pandas>=2.1,<3.0
scipy>=1.11
scikit-learn>=1.4
pillow>=10.0
tqdm>=4.66

# Headless OpenCV (서버용, system libGL 의존 없음). grad-cam이 cv2 사용.
opencv-python-headless>=4.9

# Grad-CAM
grad-cam>=1.5

# Visualization (Plotly: 대시보드, Matplotlib/Seaborn: 일부 정적 figure)
plotly>=5.20
matplotlib>=3.8
seaborn>=0.13

# Dashboard
streamlit>=1.32
