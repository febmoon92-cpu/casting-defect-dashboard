# =====================================================================
# Casting Defect Dashboard - Streamlit Cloud용 dependencies (CPU only)
#
# - PyTorch CPU 휠을 명시 (--extra-index-url)
# - 상한 핀은 제거: Cloud의 Python 버전(3.11/3.12/3.13)에 맞춰 resolver가
#   호환 휠을 자동 선택하도록 둔다.
# - opencv / grad-cam 패키지는 사용하지 않는다.
#   Grad-CAM 알고리즘은 src/gradcam.py 에 PyTorch hooks + matplotlib
#   colormap 만으로 자체 구현되어 있어, libGL 등 시스템 라이브러리에
#   대한 의존이 전혀 없다.
# =====================================================================

--extra-index-url https://download.pytorch.org/whl/cpu

# Deep learning (CPU wheels). 2.5.x 는 Python 3.9~3.13 모두 휠 제공.
torch>=2.5,<2.7
torchvision>=0.20,<0.22

# Core scientific stack
numpy>=1.26
pandas>=2.1
scipy>=1.13
scikit-learn>=1.5
pillow>=10.0
tqdm>=4.66

# Visualization
plotly>=5.20
matplotlib>=3.8
seaborn>=0.13

# Dashboard
streamlit>=1.36
