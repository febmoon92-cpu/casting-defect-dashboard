# =====================================================================
# Casting Defect Dashboard - Streamlit Cloud용 dependencies (CPU only)
#
# - PyTorch CPU 휠을 명시 (--extra-index-url)
# - 상한 핀은 제거: Cloud의 Python 버전(3.11/3.12/3.13)에 맞춰 resolver가
#   자동으로 호환 휠을 선택하도록 한다.
# - opencv는 headless 버전만 명시. grad-cam 이 비-headless opencv 를
#   transitive 로 끌어올 수 있으나, packages.txt 의 libgl1 으로 보호.
# =====================================================================

--extra-index-url https://download.pytorch.org/whl/cpu

# Deep learning (CPU wheels). 2.5.x 는 Python 3.9~3.13 모두 휠 제공.
torch>=2.5,<2.7
torchvision>=0.20,<0.22

# Core scientific stack — 하한만, Cloud Python 에 맞춰 최신 휠 자동 선택
numpy>=1.26
pandas>=2.1
scipy>=1.13
scikit-learn>=1.5
pillow>=10.0
tqdm>=4.66

# Headless OpenCV (서버용, libGL 없이도 import 가능)
opencv-python-headless>=4.10

# Grad-CAM
grad-cam>=1.5

# Visualization
plotly>=5.20
matplotlib>=3.8
seaborn>=0.13

# Dashboard
streamlit>=1.36
