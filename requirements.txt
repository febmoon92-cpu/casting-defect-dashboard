# =====================================================================
# Casting Defect Dashboard - Streamlit Cloud용 dependencies (CPU only)
#
# Streamlit Community Cloud는 CPU 인스턴스이므로 PyTorch CPU 휠을 사용합니다.
# Python 3.11 환경에서 검증됨 (Cloud Advanced settings 에서 3.11 선택 권장).
# 로컬 GPU 학습/평가용 패키지(kaggle, jupyter 등)는 requirements-dev.txt 참고.
# =====================================================================

--extra-index-url https://download.pytorch.org/whl/cpu

# Deep learning (CPU wheels). 2.5.1 은 Python 3.9~3.13 모두 CPU 휠 제공.
torch==2.5.1
torchvision==0.20.1

# Core scientific stack (numpy 2.x 는 의도적으로 미허용 — opencv/seaborn 일부 휠 호환 안정성)
numpy>=1.26,<2.2
pandas>=2.1,<2.3
scipy>=1.11,<1.15
scikit-learn>=1.4,<1.6
pillow>=10.0,<11.0
tqdm>=4.66

# Headless OpenCV (서버용, system libGL 의존 없음). grad-cam이 cv2 사용.
opencv-python-headless>=4.9,<4.11

# Grad-CAM
grad-cam==1.5.4

# Visualization
plotly>=5.20,<6.0
matplotlib>=3.8,<3.10
seaborn>=0.13

# Dashboard
streamlit>=1.36,<2.0
